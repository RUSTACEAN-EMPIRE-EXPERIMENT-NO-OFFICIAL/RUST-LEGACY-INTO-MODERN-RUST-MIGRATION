name: rust-modernizer-3os-v2

# ---------------------------------------------------------
# GitHub Release 생성 권한 (403 방지)
# ---------------------------------------------------------
permissions:
  contents: write

on:
  push:
    branches: ["main"]
  pull_request:
  schedule:
    # Rust의 최신 stable 버전을 정기적으로 확인하고 빌드하기 위해
    # 매주 월요일 01:00 UTC에 워크플로우를 실행합니다.
    - cron: '0 1 * * 1'

jobs:
  build:
    name: Build Modernizer on 3 OS
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      # dtolnay/rust-toolchain@stable을 사용하여 항상 최신 stable Rust 버전으로 빌드
      - uses: dtolnay/rust-toolchain@stable 

      # ---------------------------------------------------------
      # Build Release
      # ---------------------------------------------------------
      - name: Build (release)
        run: cargo build --release

      # ---------------------------------------------------------
      # Copy binary (OS-specific)
      # ---------------------------------------------------------
      - name: Copy binary (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          mkdir dist
          Copy-Item target/release/rust_modernizer.exe dist/rust_modernizer-windows.exe

      - name: Copy binary (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          mkdir -p dist
          cp target/release/rust_modernizer dist/rust_modernizer-macos

      - name: Copy binary (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        shell: bash
        run: |
          mkdir -p dist
          cp target/release/rust_modernizer dist/rust_modernizer-ubuntu

      # ---------------------------------------------------------
      # Prepare simple legacy example
      # ---------------------------------------------------------
      - name: Prepare legacy example
        shell: bash
        run: |
          echo 'fn main(){println!("legacy code. This should be modernized.");}' > hello.rs

      # ---------------------------------------------------------
      # Run Modernizer — CLI 사용법 수정 (출력 경로에 --output 플래그 사용)
      # ---------------------------------------------------------
      - name: Run Modernizer (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          mkdir output
          # 수정: --output 플래그 추가
          .\dist\rust_modernizer-windows.exe hello.rs --output output/modern_output-windows.rs

      - name: Run Modernizer (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          mkdir -p output
          # 수정: --output 플래그 추가
          ./dist/rust_modernizer-macos hello.rs --output output/modern_output-macos.rs

      - name: Run Modernizer (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        shell: bash
        run: |
          mkdir -p output
          # 수정: --output 플래그 추가
          ./dist/rust_modernizer-ubuntu hello.rs --output output/modern_output-ubuntu.rs

      # ---------------------------------------------------------
      # SHA256 Checksum
      # ---------------------------------------------------------
      - name: SHA256 (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          certutil -hashfile dist\rust_modernizer-windows.exe SHA256 > dist\sha256-windows.txt

      - name: SHA256 (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          shasum -a 256 dist/rust_modernizer-macos > dist/sha256-macos.txt

      - name: SHA256 (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        shell: bash
        run: |
          sha256sum dist/rust_modernizer-ubuntu > dist/sha256-ubuntu.txt

      # ---------------------------------------------------------
      # ZIP packaging
      # ---------------------------------------------------------
      - name: ZIP (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Compress-Archive -LiteralPath dist -DestinationPath rust_modernizer-windows.zip

      - name: ZIP (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: zip -r rust_modernizer-macos.zip dist

      - name: ZIP (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        shell: bash
        run: zip -r rust_modernizer-ubuntu.zip dist

      # ---------------------------------------------------------
      # Upload artifacts
      # ---------------------------------------------------------
      - uses: actions/upload-artifact@v4
        with:
          name: modernizer-${{ matrix.os }}
          path: |
            dist/
            output/
            *.zip

  # ---------------------------------------------------------
  # RELEASE JOB (after all 3 OS build jobs complete)
  # ---------------------------------------------------------
  release:
    name: Publish Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Auto-generate tag
      - name: Extract timestamp tag
        id: version
        run: |
          echo "tag=v$(date +'%Y.%m.%d-%H%M')-modernizer" >> $GITHUB_OUTPUT

      # Download build artifacts
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets/

      # Publish GitHub Release
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Rust Modernizer – 3 OS Release (${{ steps.version.outputs.tag }})
          body: |
            ## 🚀 Rust Legacy → Modern Migration Tool (3 OS Release)
            Included:
            - Windows, macOS, Linux binaries
            - ZIP packages
            - SHA256 checksums
            - OS-specific Modernizer outputs
            
            This release was built using the latest stable Rust toolchain based on a weekly schedule.
          files: release-assets/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
